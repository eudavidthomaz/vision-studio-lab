# Repository instructions

- When making repository changes, commit them on the current branch.
- After committing changes, create pull request metadata with the available PR tool.
- If a merge tool or authenticated GitHub workflow is available, merge the PR after it is created and checks have passed. If no merge capability is available, state that limitation in the final response.
